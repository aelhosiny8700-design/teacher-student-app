name: teacher_student_app
description: تطبيق للمدرسين لرفع الملفات والفيديوهات والاختبارات والتواصل مع الطلبة
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
  file_picker: ^8.0.0
  http: ^1.2.0
  url_launcher: ^6.2.5
  cloud_firestore: ^4.15.8
cupertino_icons: ^1.0.6

  # Firebase
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.4.4

  # UI & Fonts
  google_fonts: ^6.1.0

  # رفع الملفات لـ Cloudinary
  http: ^1.1.2
  file_picker: ^8.1.6
  image_picker: ^1.0.5

  # إدارة الحالة والخدمات
  provider: ^6.1.1
  intl: ^0.19.0
  cached_network_image: ^3.3.0
  url_launcher: ^6.2.2
  uuid: ^4.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  
